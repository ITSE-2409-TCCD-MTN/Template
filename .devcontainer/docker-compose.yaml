version: "3"

services:
  app:
    build:
      context: ..
      dockerfile: .devcontainer/Dockerfile

    volumes:
      - ..:/workspace:cached

    # command: /bin/sh -c "sudo /etc/init.d/postgresql start && /bin/bash"
    # try: true
    # stdin_open: true